<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.MemberMapper">

	<select id="login" parameterType="map" resultType="boolean">
		select  IF(COUNT(*) = 1, 1, 0)
		from members
		where userId = #{userId} and userPwd = #{userPwd}
	</select>

	<select id="userList" resultType="memberDto">
		select *
		from members
		order by #{userId};
	</select> 
	<!-- 
	<select id="searchUser" parameterType="map" resultMap="memberDto">
		select *
		from members
		<if test="word != null and word != ''">
			<if test="key == 'userId'">
				where ${key} = #{word}
			</if>
			<if test="key == 'userName'">
				where ${key} = #{word}
			</if>
			<if test="key == 'userAddress'">
				where ${key} = #{word}
			</if>
		</if>
	</select> -->
	
	
	<select id="lookupmember" parameterType="String" resultType="memberDto">
		select *
		from members
		where userId = #{userId}
	</select>
	<!--  -->
	<insert id="insertMember" parameterType="memberDto">
		insert into members (userId, userPwd, userName, userAddress, userPhone)
		values (#{userId}, #{userPwd}, #{userName}, #{userAddress}, #{userPhone})
	</insert>
	
	<update id="update" parameterType="memberDto">
		update members
		set userPwd = #{userPwd}, userName = #{userName}, userAddress = #{userAddress}, userPhone = #{userPhone}
		where userId = #{userId}
	</update>
	
	 <delete id="deleteMember" parameterType="String">
		delete from members
		where userId = #{userId}
	</delete>  
	
</mapper>